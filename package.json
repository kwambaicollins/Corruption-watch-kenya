{
  "name": "corruption-reporting-app",
  "version": "1.0.0",
  "description": "Backend for corruption reporting system",
  "main": "report.js",
  "scripts": {
    "start": "node report.js",
    "test": "echo \"Error: no tests specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
